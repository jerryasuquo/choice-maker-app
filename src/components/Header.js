import React from 'react'

export const Header = (props) => (
        <div className="header">
            <div className="container">
                <h1 className="header__text">{props.heading}</h1>
            </div>
        </div>
    )



